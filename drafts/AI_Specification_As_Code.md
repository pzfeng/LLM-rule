# AI 全生命周期方案沉淀与双向同步规范

## 1. 方案落地期的【沉淀】规范

- 当用户要求实现一个新功能或进行架构设计时，你【必须】遵循“文档先行”原则。
- 严禁只修改代码。你必须先在项目根目录的 `.ai/blueprints/` 下，创建一个以功能命名的方案文件（例如 `.ai/blueprints/2026-06-18-auth-system.md`）。
- 方案文件内必须包含：
    - 核心设计思路与架构
    - 受影响的文件列表 (Impacted Files)
    - 关键接口定义与数据流向
    - 遗留问题与扩展风险
- 只有当该方案文件创建/更新完毕后，你才能去编写或修改项目内的业务代码。

## 2. 方案变更期的【同步】规范（解决配置漂移）

- 当用户提出需求变更，或者你（Agent）发现当前代码与 `.ai/blueprints/` 中的方案不一致时，【严禁直接改代码】。
- 你必须执行以下“三步走同步流”：
    1. 【审计差异】：对比当前代码、历史方案文档和新需求，找出冲突点。
    2. 【更新方案】：先在对应的 `.ai/blueprints/xxx.md` 中，追加 `## 变更记录 (Change Log)`，写明：变更时间、变更原因、调整后的架构。
    3. 【同步代码】：根据更新后的方案，再行修改或生成本地代码。
- 确保任何时候，`.ai/blueprints/` 里的方案信息与本地最新代码是 100% 对齐的“孪生状态”。

## 3. 全局上下文感知（防止无方案全景信息）

- 每次开始新的对话或任务前，你必须首先主动读取 `.ai/blueprints/` 目录下的所有方案。
- 将这些方案作为当前项目的最高上下文（Single Source of Truth），结合当前代码进行整体评估，防止局部重构破坏全局设计。
