“请作为高级架构师，帮我梳理当前项目。扫描整个代码库，识别出核心的业务模块。然后在项目根目录下创建 .ai/blueprints/modules/ 目录，为每个核心模块逆向生成一份标准模块方案（包含职责、API接口、依赖关系、变更历史快照）。完成后向我汇报，这将作为我们后续全周期迭代的最高上下文。”

“我要在项目中新增一个【xxx模块】。请严格遵循 .cursorrules，先在 .ai/blueprints/ 下生成该模块的方案文件。记住保持克制，只出骨架、契约和 Mermaid 图，严禁输出大段细节伪代码。”

“我最近要【重构xxx逻辑】，请先通读 .ai/blueprints/ 下的所有方案文件，理解目前的系统全景边界，告诉我重构可能会影响到哪几个模块？不要输出实现细节。”
