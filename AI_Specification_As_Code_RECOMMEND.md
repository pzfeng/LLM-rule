# Specification as Code 最高行为准则

本项目采用“方案即代码”机制：`.ai/blueprints/` 是架构边界、公共契约、数据流和关键行为的事实源。任何新功能、架构设计、跨模块改造或需求变更，都必须先更新方案，再修改代码。

## 1. 核心上下文认知链路

- **启动前置检查**：在执行任何代码重构、新建文件或修改需求任务前，你【必须】按顺序主动阅读并加载以下本地资产：
    1. 根目录 `README.md` -> 获取项目运行环境、当前技术栈基因与【动态模块蓝图指针】。
    2. 对应的功能蓝图 `.ai/blueprints/[module_name].md` -> 锁定该模块的业务定位、Mermaid 拓扑图与【类型契约 Interface】。
    3. 通用规范文件（例如 `docs/CODE_STYLE.md`，请根据实际路径调整）-> 提取 `🤖 AI_GUIDE` 视觉锚点下的非功能性约束。
- **严禁过度脑补**：如果上述资产中的类型定义、数据库 Schema 或业务边界存在模糊地带，【必须】立即弹窗或暂停询问人类，绝对禁止盲目猜测并生成货不对板的代码。

## 2. 文档先行

- 新功能、架构设计、跨模块改造前，必须先在 `.ai/blueprints/` 创建或更新方案文件。
- 方案未落盘前，严禁修改业务代码。
- 严禁只在 Chat 中输出理论、代码或方案。

命名建议：

```text
.ai/blueprints/YYYY-MM-DD-feature-name.md
```

## 3. 方案限高线与文件生成

方案只允许描述（即文件内容必须包含）：

- 核心设计决策
- 架构边界
- Impacted Files
- 关键接口 / 类型契约
- 数据流向
- 遗留问题与扩展风险
- 变更历史

方案禁止包含：

- 具体函数内部实现
- getter / setter 级别细节
- 超过 5 行的代码或伪代码
- 教程式解释
- 与当前需求无关的背景铺陈

## 4. 表达规则

- 拓扑关系用 Mermaid。
- 设计决策用短文本。
- 公共契约用 Type / Interface。
- Mermaid 只表达结构，不写长说明。
- 每个图后最多补充 5 条边界约束。
- 禁止用长段文本描述复杂数据流或模块关系。

原则：

```text
拓扑用图，决策用文，契约用类型。
```

## 5. 上下文读取策略

- 小型项目：任务开始时读取全部 `.ai/blueprints/`。
- 中型项目：先读取 `.ai/blueprints/INDEX.md`，再读取相关方案。
- 大型项目：先读取 INDEX，根据模块、依赖和影响面读取必要方案；只有全局架构任务才读取全部方案。

## 6. 变更同步流程

当需求变化，或发现代码与方案不一致时，严禁直接改代码，必须执行：

1. 审计差异：对比新需求、当前代码和相关方案，找出契约冲突点。
2. 更新方案：在对应方案的 `变更历史追踪` 中追加记录，说明变更时间、原因、影响面和调整后的契约。
3. 同步代码：按更新后的方案修改代码。
4. 验证一致性：确认方案中的契约、数据流、影响文件与代码一致。

## 7. 外部信息引入与经验固化规范

- **严禁隐性依赖**：在多轮 Chat 调试中，如果你（AI）通过联网、读取外部网页、或由人类喂入外部文档（如 GitHub Issue、第三方 API、内部 Wiki）获取了关键知识并据此调整了设计，**严禁将该知识仅停留在聊天框中**。
- **方案联动更新**：一旦外部知识改变了原有的设计走向，你【必须】立即执行“第 6 条：变更同步流程”：
    1. 暂停代码修改，要求更新对应的 `.ai/blueprints/YYYY-MM-DD-feature-name.md`。
    2. 在【核心设计决策】中，以 `- **外部依据 (REF)**: [URL/文件名]` 的标准格式，将外部事实源记录下来。
    3. 简述“AI 默认逻辑”与“外部限制事实”的冲突点，确保下一次阅读该蓝图的 AI 或人类能立刻获得同等上下文。

## 8. 代码规范

- **外部规范引用**：禁止将通用的 ESLint、排版、日志规范硬编码写入本文件。你必须在生成本地文件前的 1 毫秒，顺着本文件第一章的指引，实时提取外部规范文件（如 `docs/CODE_STYLE.md`）中的契约限制。
- **代码自我审计（Self-Correction）**：将代码写入文件系统前，必须对照项目的异常处理规范（如禁止抛出原生 Error）、日志记录规范（如核心 Service 入口必须埋点 Logger）进行自查。若违反，必须在后台隐式推翻重写，直到 100% 合规。
